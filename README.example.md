# TODO: Project name

TODO: One-sentence description of what this project does and who it is for.

## Getting started

Install dependencies:

```bash
pnpm install
```

Start the development server:

```bash
pnpm dev
```

Open [http://localhost:3000](http://localhost:3000).

## Scripts

```bash
pnpm dev
```

Run the local development server.

```bash
pnpm build
```

Create a production build.

```bash
pnpm start
```

Run the production build locally.

```bash
pnpm lint
```

Run the project's lint checks.

```bash
pnpm test
```

Run tests.

## Project structure

TODO: Briefly document any structure or conventions that are useful to contributors.

For example:

```text
src/
├── app/          # Routes and layouts
├── components/   # Shared UI
└── lib/          # Shared utilities
```

## Deployment

TODO: Describe how this project is deployed.

If using Vercel, include the production URL and any project-specific deployment notes.

## Notes

TODO: Add anything a future contributor — including yourself in six months — would need to know to work on this project confidently.
